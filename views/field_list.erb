<section class="section section-default point">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <h3 id="buttons">(๑•̀ㅂ•́)و✧ <span style="text-decoration: underline;"><%= @db_name %></span> Database's <span style="text-decoration: underline;"><%= @layout_name %></span> Layout's Field names are below (๑•̀ㅂ•́)و✧ </h3>
        <hr>
        <div class="bs-component">
          <ul class="list-group">
            <% @field_names.each do |field_name| %>
              <li class="list-group-item">
                <h4 class="list-group-item-heading">
                  <%= field_name %>
                </h4>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
