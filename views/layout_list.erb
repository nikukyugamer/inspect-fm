<section class="section section-default point">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <h3 id="buttons">₍ (ง ˙ω˙)ว ⁾⁾ <span style="text-decoration: underline;"><%= @db_name %></span> select Layout name in Database ₍ (ง ˙ω˙)ว ⁾⁾</h3>
        <hr>
        <div class="bs-component">
          <ul class="list-group">
            <% @layout_names.each do |layout_name| %>
              <li class="list-group-item">
                <h4 class="list-group-item-heading">
                  <%= %Q(<a href="/#{@db_name}/#{layout_name}">) %>
                    <%= layout_name %>
                 </a>
                </h4>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>
